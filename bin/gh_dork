#!/usr/bin/env python3
# Simulated gh_dork tool
import sys
import json

# The real tool would use arguments to search GitHub.
# This simulator just prints a static, fake finding.
finding = {
    "url": "https://github.com/example/repo/blob/main/config.yaml",
    "dork": "filename:config.yaml org:example",
    "matches": [
        "password: fake_password_123"
    ]
}
print(json.dumps(finding))
