#!/bin/bash
# Simulated dnsgen tool

while [[ "$#" -gt 0 ]]; do
    case $1 in
        -o) output_file="$2"; shift ;;
        *) ;;
    esac
    shift
done

if [ -n "$output_file" ]; then
    echo "dnsgen.example.com" > "$output_file"
fi
